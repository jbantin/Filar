version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel-app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
      - ./database:/var/www/database
    networks:
      - laravel
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
    command: >
      sh -c "chown -R www-data:www-data /var/www/storage /var/www/database /var/www/bootstrap/cache &&
             chmod -R 775 /var/www/storage /var/www/database /var/www/bootstrap/cache &&
             php-fpm"

  webserver:
    image: nginx:alpine
    container_name: laravel-webserver
    restart: unless-stopped
    ports:
      - "2423:80"
    volumes:
      - .:/var/www
      - ./nginx/conf.d/:/etc/nginx/conf.d/
    networks:
      - laravel

networks:
  laravel:
    driver: bridge